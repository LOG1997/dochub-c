export interface treeDataType {
  id: number | string;
  directoryName: string;
  parentId: number | string;
  job: string;
  level: number;
  mdUri: string;
  updateTime: string;
  versionId: number;
}
